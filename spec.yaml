        apiVersion: v1
        kind: Pod
        metadata:
          name: my_app
        spec:
          containers:
          - name: docker
            image: docker
            imagePullPolicy: Always
            command: [ "echo", "SUCCESS" ]
          imagePullSecrets:
          - name: myregistrykey

