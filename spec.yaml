        apiVersion: v1
        kind: Pod
        spec:
          containers:
          - name: maven
            image: docker
            command:
            - docker version
              docker build hello-world
              cat
            
            tty: true
