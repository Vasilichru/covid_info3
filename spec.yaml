        apiVersion: v1
        kind: Pod
        metadata:
          name: my_app
        spec:
          containers:
          - name: docker
            image: docker
            command: [ "echo", "SUCCESS" ]
    
