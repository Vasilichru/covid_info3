        apiVersion: v1
        kind: Pod
        metadata:
          name: my_app
        spec:
          containers:
          - name: docker
            image: DOCKER_USER/DOCKER_REGISTRY_SERVER:latest
            imagePullPolicy: Always
            command: [ "echo", "SUCCESS" ]
          imagePullSecrets:
          - name: myregistrykey

    
