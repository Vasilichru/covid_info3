        apiVersion: v1
        kind: Pod
        spec:
          containers:
          - name: maven
            image: maven:3-eclipse-temurin-8-alpine
            command:
            #- docker version
            #  docker build hello-world
              cat
            
            tty: true
