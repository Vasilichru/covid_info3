        apiVersion: v1
        kind: Pod
        spec:
          containers:
          - name: maven
            image: maven:latest-alpine
            command:
            - docker version
              docker build hello-world
              cat
            
            tty: true
